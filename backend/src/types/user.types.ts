
export type UserLoginInput = {
    email: string;
    password: string;
}

export type IUser = {
    email: string,
}

export type productInput = {
    name: string;
    description: string;
    quantity: number;
    price: number;
}